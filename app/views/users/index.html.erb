<div class="container">
	<h1>All users</h1>

	<% @users.each do |u| %>
		<div>
			<img src="<%= u.avatar %>" width="50", class: "avatar-full">

			<%= link_to "#{u.name}", user_path(u.id) %>
		</div>
	<% end %>
</div>