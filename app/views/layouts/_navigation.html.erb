<div id="goddamn-navigation">
    <nav class="navbar-expand-lg navbar-light row nav-padding">
            <a class="navbar-brand"<%= link_to image_tag("logo.png", id:"logo", class:"d-inline-block align-baseline mr-2", width:"20", height:"30") + "Evalist", root_path %></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto">
                    <% if current_user.nil? %>
                    <% else %>
                    <a class="nav-link"<%= link_to 'New Project', new_project_path, method: :get %></a>
                    <% end %>
                    <a class="nav-link"<%= link_to 'View Projects', projects_path, method: :get %></a>
                    <a class="nav-link"<%= link_to 'View Users', users_path, method: :get %></a>

                    <% if current_user.nil? %>

                    <a class="nav-link"<%= link_to 'Signup', sign_up_path, method: :get %></a>
                    <a class="nav-link"<%= link_to 'Sign in', sign_in_path, method: :get %></a>
                    
                    <% else  %>
                        <!-- <li class="nav-item"><span class="navbar-text">Logged in as</span></li> -->
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            
                                <%= current_user.name  %>
                            </a>
                            <div class="dropdown-menu " aria-labelledby="navbarDropdown">
                                <a class="dropdown-item"<%= link_to 'My Profile', user_path(current_user) %> </a>
                                <a class="dropdown-item"<%= link_to 'My Projects', your_projects_path %></a>
                                <a class="dropdown-item"<%= link_to 'My Reviews', your_reviews_path %></a>


                                
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item"><%= button_to 'Sign out', sign_out_path, method: :delete, class:"btn btn-primary" %></a>

                            </div>
                        </li>
                    
                    <% end %>
                </ul>

            </div>
    </nav>

</div>
